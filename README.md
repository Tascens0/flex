Run "lune run [PATH] [TARGET]" to check you project's file count and line count.

To only match a certain pattern you can also run add a pattern as a second argument, for an example:
"lune run flex . .luau" target only files with .luau
